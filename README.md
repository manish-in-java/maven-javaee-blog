A sample application that uses JavaEE technologies. Can be deployed on any
container based on JavaEE.

## 1. Run with embedded Glassfish

`mvn package -Pglassfish`

## 2. Run with embedded TomEE

`mvn package -Ptomee`